<!--
  Placeholder config for property types that have no options yet (e.g. taxonomy).
  Keeps every type's config slot populated so TemplateProp can stay uniform.
  Extra props passed by TemplateProp (e.g. `template`) are simply ignored.
-->
<div class="mv-content-container"></div>
